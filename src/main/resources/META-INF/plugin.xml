<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.deadlocklockordercompanion</id>
    <name>Deadlock Lock-Order Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Builds a real lock acquisition-order graph from
        <code>synchronized</code> blocks/methods inside a single Java
        class, and flags a gutter warning on every site that closes a
        CYCLE in that graph -- the classic static-deadlock signal: one
        method acquires lock A then, while still holding it, acquires
        lock B; another method in the same class acquires B then A.
        If two threads hit those two methods at the same time, each
        can end up holding one lock while waiting forever for the
        other.</p>
        <p><b>A real mechanism, not a text pattern</b> -- this walks
        the actual PSI tree to find genuine lexical nesting of
        <code>synchronized</code> sites, builds a directed graph keyed
        by the lock expression text, and runs a real DFS cycle search
        over it (the textbook approach from MIT/ECOOP 2005 "Static
        Deadlock Detection for Java Libraries"). IntelliJ's own thread
        dumps only show a deadlock AFTER it happens at runtime; no
        Marketplace plugin found does static lock-order cycle detection
        ahead of time.</p>
        <p><b>v0.2: cross-method edges.</b> A lock acquired not by
        direct textual nesting but through a call to ANOTHER method of
        the same class -- <code>synchronized(a) { helper(); }</code>
        where <code>helper()</code> itself acquires lock <code>b</code>
        (directly, or transitively through its own calls) -- is now
        followed too, with real-cycle-safe memoization so a genuine
        call cycle between methods never hangs or crashes.</p>
        <p><b>v0.3: cross-CLASS edges through an injected collaborator.</b>
        A call like <code>synchronized(a) { collaborator.helper(); }</code>,
        where <code>collaborator</code> is a field/parameter of the
        caller's class, is now followed into the collaborator's own
        lock graph -- the most common real-production shape (a service
        calling into another injected service). An interface/abstract
        collaborator type is followed only when exactly ONE class in
        the same module implements it; the chain is bounded to 3
        collaborator classes deep, an explicit documented cost bound.</p>
        <p>Stated honestly: lock identity is matched by exact expression
        text (<code>lockA</code> and <code>this.lockA</code> are treated
        as different locks). Java only;
        <code>ReentrantLock</code>/<code>java.util.concurrent</code>
        locks and Kotlin's <code>synchronized(){}</code> are not yet
        covered.</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.java</depends>

    <extensions defaultExtensionNs="com.intellij">
        <codeInsight.lineMarkerProvider language="JAVA"
            implementationClass="dev.gaphunter.deadlocklockordercompanion.gutter.LockOrderCycleLineMarkerProvider"/>
        <notificationGroup id="Deadlock Lock-Order Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
